apiVersion: v1
kind: Namespace
metadata:
  name: health-alpha
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: health-api
  namespace: health-alpha
spec:
  replicas: 1
  selector:
    matchLabels:
      app: health-api
      env: alpha
  template:
    metadata:
      labels:
        app: health-api
        env: alpha
    spec:
      containers:
      - name: health-api
        image: REPLACE_IMAGE
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 2
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: health-api
  namespace: health-alpha
spec:
  selector:
    app: health-api
    env: alpha
  ports:
  - port: 80
    targetPort: 8080
